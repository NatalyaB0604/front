import React from 'react';
import Typography from '@mui/material/Typography';

const Agreement = () => {
  return (
    <div>
      <Typography variant="h4">Публичный договор</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        Здесь размещен текст публичного договора.
      </Typography>
    </div>
  );
};

export default Agreement;
